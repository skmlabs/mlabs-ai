node_modules/
.next/
out/
build/
.env*.local
.env
!.env.example
.vercel
.DS_Store
*.log
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache
# Decision (Prompt 1A): tsc incremental build artifact; not in prompt's gitignore but obviously shouldn't be committed
*.tsbuildinfo
# Decision (Prompt 1B): local Claude Code state — not shared
.claude/
